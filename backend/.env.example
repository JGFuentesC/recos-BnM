# Backend local configuration
# Copy this file to backend/.env and fill local-only values.
# Do not commit backend/.env or serviceAccountKey.json.

GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
FIREBASE_PROJECT_ID=proyectofinal-71637
PORT=3001

# Optional external APIs, only if this backend process needs them.
TMDB_API_KEY=
GOOGLE_BOOKS_API_KEY=

# Optional emulator mode. Keep disabled unless explicitly testing Firebase emulators.
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
